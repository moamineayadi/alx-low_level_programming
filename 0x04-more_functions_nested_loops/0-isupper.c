#include "main.h"

/**
 * _isupper - Function that checks a char if it's uppercase
 * @c: int type number
 * Return: 1 if c is uppercase else 0
 */

int _isupper(int c)
{
	return ((c >= 65) && (c <= 90));
}
