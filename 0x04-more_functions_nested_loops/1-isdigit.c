#include "main.h"

/**
 * _isdigit - function that checks a char is a digit or not
 * @c: int type number
 * Return: 1 if c is digit else 0
 */

int _isdigit(int c)
{
	return ((c >= 48) && (c <= 57));
}
